apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Address relative to the internal docker network
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    url: http://loki:3100
    access: proxy
    editable: true
